<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SustainWear — Forgot Password</title>
    <meta name="description" content="Reset your SustainWear account password">  
    <style>
        :root {
            --primary: #27ae60;
            --primary-dark: #219150;
            --bg-light: #f4fbf6;
            --text-dark: #222;
            --card-bg: #fff;
            --border: #cce5d0;
            --input-border: #b2d8b2;
            --error: #e74c3c;
            --input-padding: 0.75rem;
        }

        html, body {
            height: 100%;
        }

        body {
            font-family: 'Segoe UI', sans-serif;
            background: var(--bg-light);
            min-height: 100vh;
            margin: 0;
        }

        .login-container {
            background: var(--card-bg);
            border-radius: 18px;
            box-shadow: 0 8px 40px 0 rgba(39, 174, 96, 0.18), 0 2px 8px 0 rgba(0,0,0,0.06);
            padding: 3.2rem 2.7rem 2.7rem;
            max-width: 430px;
            width: 100%;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            border: 2.5px solid var(--border);
            margin: 3.5rem auto 0 auto;
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        h1#forgotTitle {
            color: var(--primary);
            font-size: 2.1rem;
            margin-bottom: 0.2rem;
            font-weight: 800;
            letter-spacing: 0.01em;
        }
        .lead {
            color: #4a7c5b;
            font-size: 1.13rem;
            margin-bottom: 1.5rem;
            font-weight: 500;
        }

        .messages {
            margin-bottom: 1.2rem;
        }
        .msg-success {
            color: var(--primary);
            background: #eafaf1;
            border: 1px solid var(--input-border);
            border-radius: 4px;
            padding: 0.7rem 1rem;
            margin-bottom: 0.5rem;
            text-align: center;
        }
        .msg-error {
            color: var(--error);
            background: #fff0ee;
            border: 1px solid #f5c6cb;
            border-radius: 4px;
            padding: 0.7rem 1rem;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .form-group {
            margin-bottom: 1.25rem;
            display: flex;
            flex-direction: column;
        }

        label {
            font-size: 1.13rem;
            margin-bottom: 0.5rem;
            color: var(--text-dark);
            font-weight: 600;
            letter-spacing: 0.01em;
        }

        input[type="email"] {
            width: 100%;
            padding: 1.1rem 1rem;
            border: 1.5px solid var(--input-border);
            border-radius: 6px;
            font-size: 1.13rem;
            box-sizing: border-box;
            background: #f8fcf9;
            transition: border-color 0.2s, box-shadow 0.2s;
            outline: none;
            color: var(--text-dark);
            font-weight: 500;
        }

        input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 2px #b2d8b2;
            background: #fff;
        }

        input:active {
            border-color: var(--primary-dark);
        }

        input[aria-invalid="true"] {
            border-color: var(--error);
            box-shadow: 0 0 0 2px #f5c6cb;
        }

        .submit {
            background: var(--primary);
            color: #fff;
            padding: 1.05rem 0;
            width: 100%;
            border-radius: 4px;
            border: none;
            font-size: 1.18rem;
            cursor: pointer;
            transition: background-color 0.3s;
            font-weight: 700;
            letter-spacing: 0.01em;
        }

        .submit:hover {
            background: var(--primary-dark);
        }

        .center {
            text-align: center;
            margin-top: 1.2rem;
            font-size: 1.05rem;
        }

        @media (max-width: 540px) {
            .login-container {
                width: 99vw;
                padding: 1.2rem 0.5rem;
                max-width: 99vw;
            }
            h1#forgotTitle {
                font-size: 1.5rem;
            }
            label, input, .submit, .center {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <main class="login-container" role="main" aria-labelledby="forgotTitle">
        <h1 id="forgotTitle">Forgot Password</h1>
        <p class="lead">Enter the email associated with your account and we'll send reset instructions.</p>

        <div class="messages" aria-live="polite">
            <% if (success && success.length > 0) { %>
                <div class="msg-success"><%- success[0] %></div>
            <% } %>
            <% if (message) { %>
                <div class="msg-error"><%- message %></div>
            <% } %>
        </div>

        <form action="/forgotpassword" method="POST" novalidate>
            <% if (typeof csrfToken !== 'undefined') { %>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>

            <div class="form-group">
                <label for="email">Email address</label>
                <input id="email" name="email" type="email" autocomplete="email" required placeholder="you@email.com" value="<%= typeof formData !== 'undefined' && formData.email ? formData.email : '' %>">
            </div>

            <button class="submit" type="submit">Send reset link</button>
        </form>

        <p class="center"><a href="/login">Back to Login</a></p>
    </main>
</body>
</html>
